services:
  frontend:
    build:
      context: frontend
      target: development
    ports:
      - 3000:3000
    # volumes:
    #   - ./frontend:/usr/src/app
    #   - /usr/src/app/node_modules
    container_name: frontend
    restart: always
    networks:
      - frontend
    # depends_on:
    #   - backend
  backend:
    build:
      context: backend
      target: development
    ports:
      - 8000:8000
    # volumes:
    #   - ./frontend:/usr/src/app
    #   - /usr/src/app/node_modules
    container_name: backend
    # restart: always
    networks:
      - backend
    # depends_on:
    #   - backend



networks:
  frontend:
  backend:
